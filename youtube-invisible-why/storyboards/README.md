# storyboards/

Output of the Storyboard Agent (agents/06-storyboard-agent.md): `<video-id>.json`, consumed directly by remotion-engine (`--props=storyboards/<video-id>.json`). See pilots/*/storyboard.json for three worked, render-tested examples.
