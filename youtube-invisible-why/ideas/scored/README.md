# ideas/scored

Ideas that passed the Idea Scorer (agents/02-idea-scorer.md, scoring/score_idea.js) with verdict `pass` or `maybe`. `maybe` (60-74) needs a human look before it proceeds; `pass` (>=75) continues automatically to the Research Agent.
